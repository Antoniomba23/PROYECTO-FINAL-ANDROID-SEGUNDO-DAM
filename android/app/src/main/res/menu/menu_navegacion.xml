<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Navegación principal -->
    <group android:id="@+id/grupo_nav">
        <item
            android:id="@+id/nav_inicio"
            android:icon="@drawable/ic_home"
            android:title="Inicio" />

        <item
            android:id="@+id/nav_perfil"
            android:icon="@drawable/ic_person"
            android:title="Mi Perfil" />
        <item
            android:id="@+id/nav_cambiar_centro"
            android:icon="@drawable/ic_location"
            android:title="Cambiar centro" />
    </group>

    <!-- Administración -->
    <group android:id="@+id/grupo_admin">
        <item
            android:id="@+id/nav_panel_admin"
            android:icon="@drawable/ic_person"
            android:title="Panel de Administración"
            android:visible="false" />
    </group>

    <!-- Sesión -->
    <group android:id="@+id/grupo_sesion">
        <item
            android:id="@+id/nav_login"
            android:icon="@drawable/ic_login"
            android:title="Iniciar sesión" />
        <item
            android:id="@+id/nav_logout"
            android:icon="@drawable/ic_logout"
            android:title="Cerrar sesión" />
    </group>

</menu>
