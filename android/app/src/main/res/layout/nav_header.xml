<?xml version="1.0" encoding="utf-8"?>
<!--
    Cabecera del Navigation Drawer.
    Muestra el nombre de la app y el email del usuario (o "Modo Invitado").
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="176dp"
    android:background="@color/primary"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- Logo de la app -->
    <ImageView
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@drawable/logo"
        android:scaleType="centerInside"
        android:contentDescription="Logo StudyBro"/>

    <!-- Nombre app -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="StudyBro"
        android:textColor="@color/on_primary"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginTop="8dp"/>

    <!-- Email del usuario -->
    <TextView
        android:id="@+id/tvNavEmail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Modo Invitado"
        android:textColor="@color/on_primary"
        android:alpha="0.85"
        android:textSize="13sp"/>

    <!-- Centro del usuario -->
    <TextView
        android:id="@+id/tvNavCentro"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="@color/on_primary"
        android:alpha="0.65"
        android:textSize="11sp"/>

</LinearLayout>
